version: '3.8'

services:
  vedic-astro:
    build: .
    ports:
      - "8080:8080"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
    volumes:
      - ./static:/app/static
      - ./templates:/app/templates
